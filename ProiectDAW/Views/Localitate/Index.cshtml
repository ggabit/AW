@model IEnumerable<ProiectDAW.Models.Localitate>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Denumire)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Judet.Denumire)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Denumire)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Judet.Denumire)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.LocalitateID }) |
            @Html.ActionLink("Details", "Details", new { id=item.LocalitateID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.LocalitateID })
        </td>
    </tr>
}

</table>
