@model ProiectDAW.Models.Localitate

@{
    ViewBag.Title = "Ștergere";
}

<h2>@ViewBag.Title</h2>

<h3>Confirmați ștergerea?</h3>
<div>
    <h4>@Model.Denumire</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Denumire)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Denumire)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Judet.Denumire)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Judet.Denumire)
        </dd>

    </dl>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

<div class="form-actions no-color">
    <input type="submit" value="Șterge" class="btn btn-danger" /> |
    @Html.ActionLink("Înapoi", "Edit", "Judet", new { id = Model.JudetID }, new { @class = "btn btn-primary" })
</div>
    }
</div>
