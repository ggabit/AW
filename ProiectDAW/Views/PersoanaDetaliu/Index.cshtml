@model IEnumerable<ProiectDAW.Models.PersoanaDetaliu>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Adresa)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LocuintaCurte)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Persoana.Nume)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Localitate.Denumire)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Adresa)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LocuintaCurte)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Persoana.Nume)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Localitate.Denumire)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PersoanaId }) |
            @Html.ActionLink("Details", "Details", new { id=item.PersoanaId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PersoanaId })
        </td>
    </tr>
}

</table>
